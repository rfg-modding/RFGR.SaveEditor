{
  "scripts": {
    "build": "npx @tailwindcss/cli -i wwwroot/input.css -o wwwroot/output.css",
    "watch": "npx @tailwindcss/cli -i wwwroot/input.css -o wwwroot/output.css --watch"
  },
  "devDependencies": {
    "stylelint": "^16.14.1",
    "stylelint-config-standard": "^37.0.0"
  }
}
